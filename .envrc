dotenv

if command -v supabase >/dev/null 2>&1; then
  supabase_token_file="${XDG_CONFIG_HOME:-$HOME/.config}/supabase/access_token"
  if [ ! -s "$supabase_token_file" ]; then
    bash ./scripts/dev/ensure-supabase-auth.sh >/dev/null 2>&1 || true
  fi
fi
